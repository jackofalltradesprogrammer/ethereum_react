const routes = require("next-routes")(); // we need to invoke the funcion returned by next-routes


// this will help export helpers for components to navigate with the routing
module.exports = routes; 